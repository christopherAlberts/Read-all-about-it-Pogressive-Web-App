import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'read_all_about_it',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
